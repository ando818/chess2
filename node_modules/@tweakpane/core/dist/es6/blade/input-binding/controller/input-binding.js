import { LabelController } from '../../label/controller/label';
/**
 * @hidden
 */
export class InputBindingController extends LabelController {
    constructor(doc, config) {
        super(doc, config);
        this.binding = config.binding;
    }
}
